from uuid import uuid4


def ingest_syllabus(filename: str) -> str:
    """
    Stub for PDF/Doc ingestion pipeline.
    Replace with:
    1) file extraction
    2) chunking
    3) embeddings
    4) persistence in DB + vector store
    """
    _ = filename
    return str(uuid4())
